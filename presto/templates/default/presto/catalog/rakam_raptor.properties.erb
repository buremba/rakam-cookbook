connector.name=rakam_raptor

backup.timeout=20m

backup.provider=s3
aws.s3-bucket=<%= node["s3"]["batch"] %>
aws.region=<%= node["opsworks"]["instance"]["region"] %>

metadata.db.type=mysql
metadata.db.url=jdbc:mysql://<%= node["opsworks"]["stack"]["rds_instances"][0]["address"] %>/<%= node["opsworks"]["stack"]["rds_instances"][0]["db_name"] %>?user=<%= node["opsworks"]["stack"]["rds_instances"][0]["master_username"] %>&password=<%= node["deploy"]["presto"]['environment_variables']["DB_PASSWORD"] %>
metadata.db.connections.max=100

storage.data-directory=var/data

#cache.data-size=2GB
#cache.method=memory_mapped
